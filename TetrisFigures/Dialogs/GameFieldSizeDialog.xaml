<Window x:Class="TetrisFigures.Dialogs.GameFieldSizeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TetrisFigures.Dialogs"
        mc:Ignorable="d"
        Title="GameFieldSizeDialog" Height="250" Width="400">
    <Window.Resources>
        <Style TargetType="{x:Type FrameworkElement}">
            <Setter Property="Margin" Value="5 5 5 5"/>
        </Style>
        <Style x:Key="TextBoxErrorStyle" TargetType="{x:Type TextBox}">
            <Setter Property="Validation.ErrorTemplate">
                <Setter.Value>
                    <ControlTemplate x:Name="TextBoxErrorTemplate">
                        <AdornedElementPlaceholder>
                                <Border BorderBrush="Red" BorderThickness="2"/>
                         </AdornedElementPlaceholder>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="Validation.HasError" Value="True">
                    <Setter Property="ToolTip" Value="{Binding RelativeSource={x:Static RelativeSource.Self}, Path=(Validation.Errors)[0].ErrorContent}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Window.CommandBindings>
        <CommandBinding Command="Save" Executed="SaveCommand_Executed" CanExecute="SaveCommand_CanExecute"/>
    </Window.CommandBindings>
    <Grid Margin="5 5 5 5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="6*"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="6*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Label Grid.Column="0" Grid.Row="0" Content="Gamefield width:" VerticalContentAlignment="Center" 
               HorizontalContentAlignment="Center" Target="{Binding ElementName=bWidth}"/>
        <Label Grid.Column="0" Grid.Row="1" Content="Gamefield height:" VerticalContentAlignment="Center"
               HorizontalContentAlignment="Center" Target="{Binding ElementName=bHeight}"/>
        <Label Grid.Column="0" Grid.Row="2" Content="Ratio:" VerticalContentAlignment="Center" 
               HorizontalContentAlignment="Center" Target="{Binding ElementName=lbRatio}"/>
        <TextBox Grid.Column="2" Grid.Row="0" x:Name="bWidth" Text="{Binding Path=sz.width, NotifyOnValidationError=True,
            Mode=TwoWay, ValidatesOnExceptions=True, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TextBoxErrorStyle}"/>
        <TextBox Grid.Column="2" Grid.Row="1" x:Name="bHeight" Text="{Binding Path=sz.height, NotifyOnValidationError=True,
            Mode=TwoWay, ValidatesOnExceptions=True, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TextBoxErrorStyle}"/>
        <Label Grid.Column="2" Grid.Row="2" x:Name="lbRatio" Content="{Binding Path=sz.ratio, Mode=OneWay}"/>
        <Button Grid.Column="0" Grid.Row="3" x:Name="btnOK" IsCancel="False" IsDefault="True" Content="OK" Command="Save"/>
        <Button Grid.Column="2" Grid.Row="3" x:Name="btnCancel" IsCancel="True" IsDefault="False" Content="Cancel"/>
    </Grid>
</Window>